/* LIW Cards — STAGING ONLY emergency rollback — 2026-08-14.
   The selected-status experiment is intentionally disabled after causing the
   editor to become unresponsive. Kept as a no-op so any cached loader that
   still requests this file receives safe code. */
(function(){ 'use strict'; return; })();
