(function(){
'use strict';
const $=s=>document.querySelector(s);let user=null,session=null,orders=[],filtered=[],selected=null,intake=null,assets=[],messages=[],history=[],cards=[];
const statusLabels={awaiting_intake:'Awaiting intake',intake_submitted:'Intake submitted',in_design:'In design',customer_review:'Customer review',revision_requested:'Revision requested',approved:'Approved',publishing:'Publishing',completed:'Completed',on_hold:'On hold',canceled:'Canceled'};
const notifiableStatuses=new Set(['intake_submitted','in_design','customer_review','revision_requested','approved','publishing','completed','on_hold','canceled']);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const dt=v=>v?new Date(v).toLocaleString(undefined,{month:'short',day:'numeric',year:'numeric',hour:'numeric',minute:'2-digit'}):'—';
function alertBox(type,msg){const n=$('#dw-admin-alert');if(!n)return;n.className=`dw-alert show ${type}`;n.textContent=msg;}
function clearAlert(){const n=$('#dw-admin-alert');if(n)n.className='dw-alert';}
async function requireAdmin(){const access=await getLiwAccessContext();if(!access?.user||!access?.isAdmin){location.href=liwUrl('dashboard.html');return false}user=access.user;const r=await supabaseClient.auth.getSession();session=r.data.session;return true;}
async function notifyDesigner(eventKey,orderId){if(!session?.access_token||!orderId||!notifiableStatuses.has(eventKey))return;const res=await fetch(`${LIW_CONFIG.supabaseUrl}/functions/v1/notify-designer-order`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${session.access_token}`,'apikey':LIW_CONFIG.supabaseKey},body:JSON.stringify({orderId,eventKey})});if(!res.ok){const data=await res.json().catch(()=>({}));throw new Error(data.error||'Designer notification could not be sent');}}
async function loadOrders(){const {data,error}=await supabaseClient.from('designer_orders').select('*').order('created_at',{ascending:false});if(error)throw error;orders=data||[];$('#dw-admin-count').textContent=`${orders.filter(o=>!['completed','canceled'].includes(o.workflow_status)).length} active`;filterOrders();}
function filterOrders(){const q=$('#dw-search')?.value.trim().toLowerCase()||'',s=$('#dw-status-filter')?.value||'';filtered=orders.filter(o=>(!s||o.workflow_status===s)&&(!q||[o.order_number,o.customer_email,o.service_name,o.domain_name].some(v=>String(v||'').toLowerCase().includes(q))));renderQueue();}
function renderQueue(){const wrap=$('#dw-queue-list');wrap.innerHTML=filtered.length?filtered.map(o=>`<button class="dw-queue-item ${selected?.id===o.id?'active':''}" type="button" data-id="${o.id}"><strong>${esc(o.order_number)} · ${esc(o.service_name)}${o.is_test?' · QA':''}</strong><span><b>${esc(statusLabels[o.workflow_status]||o.workflow_status)}</b><time>${dt(o.created_at).split(',')[0]}</time></span><span><b>${esc(o.customer_email||'No email')}</b><em>${o.revisions_used||0}/${o.revision_limit||0} revisions</em></span></button>`).join(''):'<div class="dw-empty" style="padding:28px 10px"><p>No orders match this filter.</p></div>';wrap.querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>openOrder(b.dataset.id)));}
async function openOrder(id){clearAlert();const found=orders.find(o=>o.id===id);if(!found)return;selected=found;renderQueue();$('#dw-no-selection').hidden=true;$('#dw-detail').hidden=false;const [i,a,m,h,c]=await Promise.all([supabaseClient.from('designer_intakes').select('*').eq('order_id',id).maybeSingle(),supabaseClient.from('designer_order_assets').select('*').eq('order_id',id).order('created_at'),supabaseClient.from('designer_order_messages').select('*').eq('order_id',id).order('created_at'),supabaseClient.from('designer_order_status_history').select('*').eq('order_id',id).order('created_at',{ascending:false}),supabaseClient.from('digital_cards').select('id,slug,full_name,company_name,status').eq('user_id',found.user_id).order('updated_at',{ascending:false})]);for(const r of [i,a,m,h,c])if(r.error)throw r.error;intake=i.data;assets=a.data||[];messages=m.data||[];history=h.data||[];cards=c.data||[];renderDetail();}
function kv(label,value){return `<div><small>${esc(label)}</small><strong>${esc(value||'—')}</strong></div>`}
function renderDetail(){if(!selected)return;$('#dw-detail-title').textContent=`${selected.order_number} · ${selected.service_name}${selected.is_test?' · QA TEST':''}`;$('#dw-detail-subtitle').textContent=`${selected.customer_email||'No email'} · opened ${dt(selected.created_at)}`;$('#dw-detail-status').textContent=statusLabels[selected.workflow_status]||selected.workflow_status;$('#dw-order-kv').innerHTML=[kv('Payment',selected.is_test?'QA paid simulation — no Stripe charge':selected.payment_status==='paid'?'Paid / verified':selected.payment_status),kv('Service price',new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format((selected.service_price_cents||0)/100)),kv('Requested plan',selected.selected_plan_name||selected.selected_plan_key||'Current plan'),kv('Domain',selected.domain_mode==='liw'?'LIW card link':selected.domain_name||selected.domain_mode),kv('Revisions',`${selected.revisions_used||0} used of ${selected.revision_limit||0}`),kv('Assigned',selected.assigned_to?'LIW team member':'Unassigned')].join('');
 const sel=$('#dw-card-select');sel.innerHTML='<option value="">Link LIW card…</option>'+cards.map(c=>`<option value="${c.id}" ${selected.card_id===c.id?'selected':''}>${esc(c.full_name||c.company_name||c.slug)} · ${esc(c.status)}</option>`).join('');$('#dw-status-select').value=selected.workflow_status==='awaiting_intake'?'intake_submitted':selected.workflow_status;const edit=$('#dw-edit-card');if(selected.card_id){edit.hidden=false;edit.href=`editor.html?id=${encodeURIComponent(selected.card_id)}`}else edit.hidden=true;renderIntake();renderAssets();renderMessages();renderHistory();window.lucide?.createIcons?.();}
function renderIntake(){const wrap=$('#dw-intake-detail');if(!intake){wrap.innerHTML='<div style="grid-column:1/-1"><small>Status</small><strong>Customer has not submitted intake yet.</strong></div>';return}const colors=Array.isArray(intake.brand_colors)?intake.brand_colors.join(' · '):'';const social=Array.isArray(intake.social_links)?intake.social_links.join(' · '):JSON.stringify(intake.social_links||{});const services=Array.isArray(intake.services)?intake.services.join(' · '):'';const products=Array.isArray(intake.products)?intake.products.join(' · '):'';wrap.innerHTML=[kv('Contact name',intake.contact_name),kv('Business / brand',intake.business_name),kv('Title',intake.job_title),kv('Email',intake.contact_email),kv('Phone',intake.phone),kv('Website',intake.website),kv('Address',intake.business_address),kv('What they do',intake.business_description),kv('Main goal',intake.primary_goal),kv('Audience',intake.target_audience),kv('Style',intake.preferred_style||'LIW choice'),kv('Template',intake.preferred_template||'LIW choice'),kv('Brand colors',colors),kv('Color notes',intake.color_notes),kv('Booking',intake.booking_url),kv('Payment',intake.payment_url),kv('Social',social),kv('Services',services),kv('Products',products),kv('Special requests',intake.special_requests)].join('');}
async function openAsset(a){const {data,error}=await supabaseClient.storage.from('designer-assets').createSignedUrl(a.storage_path,300);if(error)return alertBox('error',error.message);window.open(data.signedUrl,'_blank','noopener');}
function renderAssets(){const w=$('#dw-admin-assets');if(!assets.length){w.innerHTML='<div class="dw-message"><p>No files were uploaded with this intake.</p></div>';return}w.innerHTML=assets.map(a=>`<div class="dw-asset"><span class="dw-asset-icon"><i data-lucide="${a.mime_type==='application/pdf'?'file-text':'image'}" size="16"></i></span><button type="button" data-asset="${a.id}" style="border:0;background:transparent;text-align:left;min-width:0;cursor:pointer"><strong>${esc(a.original_name)}</strong><small>${esc(a.asset_type.replaceAll('_',' '))}</small></button><i data-lucide="external-link" size="15"></i></div>`).join('');w.querySelectorAll('[data-asset]').forEach(b=>b.addEventListener('click',()=>openAsset(assets.find(a=>a.id===b.dataset.asset))));window.lucide?.createIcons?.();}
function renderMessages(){const w=$('#dw-admin-thread');w.innerHTML=messages.length?messages.map(m=>`<div class="dw-message ${m.is_internal?'dw-internal':''}"><div class="dw-message-head"><strong>${m.is_internal?'Internal LIW note':m.sender_role==='customer'?'Customer':m.sender_role==='system'?'System':'LIW'}</strong><time>${dt(m.created_at)}</time></div><p>${esc(m.message)}</p></div>`).join(''):'<div class="dw-message"><p>No messages yet.</p></div>';}
function renderHistory(){const w=$('#dw-history');w.innerHTML=history.length?history.map(h=>`<div class="dw-timeline-row done"><span class="dw-timeline-dot"><i data-lucide="history" size="13"></i></span><div><strong>${esc(statusLabels[h.to_status]||h.to_status)}</strong><small>${dt(h.created_at)}${h.note?' · '+esc(h.note):''}</small></div></div>`).join(''):'<div class="dw-message"><p>No status history yet.</p></div>';window.lucide?.createIcons?.();}
async function transition(){if(!selected)return;const status=$('#dw-status-select').value,note=$('#dw-status-note').value.trim(),cardId=$('#dw-card-select').value||null;const btn=$('#dw-update-status');btn.disabled=true;try{const orderId=selected.id;const {data,error}=await supabaseClient.rpc('admin_transition_designer_order',{p_order_id:orderId,p_status:status,p_note:note||null,p_card_id:cardId});if(error)throw error;selected=data;$('#dw-status-note').value='';let notice='';try{await notifyDesigner(status,orderId);}catch(notificationError){console.warn('Designer status notification failed:',notificationError);notice=' Customer email could not be sent; the order status is still saved.';}alertBox(notice?'info':'success',`Order moved to ${statusLabels[status]||status}.${notice}`);await loadOrders();await openOrder(orderId);}catch(e){alertBox('error',e.message||'Could not update order.')}finally{btn.disabled=false}}
async function addMessage(e){e.preventDefault();if(!selected)return;const input=$('#dw-admin-message'),msg=input.value.trim();if(!msg)return;const internal=$('#dw-internal-note').checked,btn=e.submitter;btn.disabled=true;try{const {error}=await supabaseClient.from('designer_order_messages').insert({order_id:selected.id,user_id:user.id,sender_role:'admin',message_type:internal?'note':'message',message:msg,is_internal:internal});if(error)throw error;input.value='';$('#dw-internal-note').checked=false;await openOrder(selected.id);}catch(err){alertBox('error',err.message||'Could not add message.')}finally{btn.disabled=false}}
async function init(){window.lucide?.createIcons?.();if(!await requireAdmin())return;try{await loadOrders();$('#dw-admin-loading').hidden=true;$('#dw-admin-app').hidden=false;$('#dw-search').addEventListener('input',filterOrders);$('#dw-status-filter').addEventListener('change',filterOrders);$('#dw-refresh').addEventListener('click',loadOrders);$('#dw-update-status').addEventListener('click',transition);$('#dw-admin-message-form').addEventListener('submit',addMessage);const fromUrl=new URLSearchParams(location.search).get('id');if(fromUrl&&orders.some(o=>o.id===fromUrl))await openOrder(fromUrl);else if(orders.length)await openOrder(orders[0].id);window.lucide?.createIcons?.();}catch(e){console.error(e);$('#dw-admin-loading').hidden=true;alertBox('error',e.message||'Could not load the designer production queue.')}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();